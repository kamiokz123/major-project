<% layout('./layouts/boilerplate.ejs') -%>
    <div class="row">
        <div class="col-8 offset-2 mt-1">
            <h3>Edit list</h3>
            <form action="/listings/<%= list._id %>?_method=PUT" method="post">

                <div class="mb-2">
                    <label for="title" class="form-label">Title :</label>
                    <input name="listing[title]" value="<%= list.title %>" class="form-control" type="text">
                </div>

                <div class="mb-2">
                    <label for="description" class="form-label">Description :</label>
                    <textarea name="listing[description]" class="form-control"><%= list.description %></textarea>
                </div>

                <div class="mb-2">
                    <label for="image" class="form-label">Image :</label>
                    <input name="listing[image]" value="<%= list.image.url %>" class="form-control" type="text">
                </div>

                <div class="mb-2">
                    <label for="price" class="form-label">Price :</label>
                    <input name="listing[price]" value="<%= list.price %>" class="form-control" type="number">
                </div>

                <div class="mb-2">
                    <label for="location" class="form-label">Location :</label>
                    <input name="listing[location]" value="<%= list.location %>" class="form-control" type="text">
                </div>

                <div class="mb-2">
                    <label for="country" class="form-label">Country :</label>
                <input name="listing[country]" class="form-control" value="<%= list.country %>" type="text">
                </div>
                <br>
                <button class="btn bt">Edit</button>
                <br>
            </form>
        </div>
    </div>